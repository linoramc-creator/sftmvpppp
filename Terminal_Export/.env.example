# ── Supabase (existing analyze-ticker edge function) ───────────────────
VITE_SUPABASE_URL=https://YOUR-PROJECT.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=your-anon-or-publishable-key

# ── Options service (new, additive — options-service/) ─────────────────
# URL of the Python FastAPI options microservice. Leave empty to hide/disable
# the Opciones section gracefully (it shows a config hint instead of erroring).
#   local dev:   http://localhost:8001
#   production:  https://your-options-service.up.railway.app
VITE_OPTIONS_API_URL=http://localhost:8001
